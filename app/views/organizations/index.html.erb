<div class="tab-content default-tab" id="tab1"> <!-- This is the target div. id must match the href of this div's tab -->
  <table>
    <thead>
      <tr>
        <th><input class="check-all" type="checkbox" /></th>
        <th>NOMBRE</th>
        <th>CATEGORIA</th>
        <th>AREA DE OPERACIÓN</th>
        <th>LUGAR DE REUNIÓN</th>
        <th>MODUS OPERANDI</th>
      </tr>

    </thead>

    <tfoot>
      <tr>
        <td colspan="6">
          <div class="bulk-actions align-left">
          </div>
          <%= render "layouts/pagination" %>
          <div class="clear"></div>
        </td>
      </tr>
    </tfoot>

    <tbody>
      <% @organizations.each do |organization| %>
        <tr>
          <%if can? :show, Organization %>
            <td><%=link_to organization.id, organization, :class => 'button'%></td>
          <%else%>
            <td><%= label organization.id, organization.id, :class => 'button'%></td>
          <%end%>


          <% @model = organization %> 

          <td><%= organization.name.upcase if organization.name %></td>

          <td><%= organization.category.name.upcase if organization.category %></td>

          <td><%= organization.operational_area.upcase if organization.operational_area %></td>

          <td><%= organization.meeting_place.upcase if organization.meeting_place %></td>

          <td><%= organization.modus_operandi %></td>
          <%= render "layouts/index_links" %>
        <% end %>
      </tr>
    </tbody>
  </table>
  <%= render "layouts/action_links" %>
</div> 
<%= render "layouts/second_tab" %>